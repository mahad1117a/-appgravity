# Apps Gravity — Environment Configurations Example
# Copy this file to .env and fill in the real values before deploying.

# Port the Express server listens on
PORT=3000

# "production" enables secure cookies, strict security checks, and hides error details from responses.
NODE_ENV=production

# Comma-separated list of origins allowed to call the API (no trailing slashes).
# ⚠️ BEFORE DEPLOYING: Replace these with your real domain(s) e.g. https://appsgravity.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://appsgravity.com

# Number of reverse proxy hops in front of this app (e.g. Nginx = 1, Railway/Render = 1, Cloudflare = 1).
# Set to 0 if the app is exposed directly with no reverse proxy.
TRUST_PROXY=1

# Web3Forms API Access Key for secure server-side form forwarding notifications.
# Obtain a free access key from https://web3forms.com
WEB3FORMS_ACCESS_KEY=your_web3forms_access_key_here

# ── ADMIN DASHBOARD (/admin.html) AUTH ──
# Admin password salts and scrypt hashes are used to secure the administrative login without storing plain passwords.
# To change/generate your password hash and salt, run the following command in your terminal:
#   node -e "const c=require('crypto');const s=c.randomBytes(16).toString('hex');console.log('ADMIN_PASSWORD_SALT='+s);console.log('ADMIN_PASSWORD_HASH='+c.scryptSync('YOUR_NEW_PASSWORD_HERE',s,64).toString('hex'))"
# Copy the printed lines below:
ADMIN_PASSWORD_SALT=80b615d460543dff10d9db900e403a72
ADMIN_PASSWORD_HASH=69625a48d2d256d46fc78280b4787293a3733f3055dc4c0ab68911d4a375b534e429d16ecff30e3b52e4b945d48c621db190bdfe3351f0ed776740c7ff88dfa7
